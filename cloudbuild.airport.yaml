steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
    - 'build'
    - '-f'
    - 'Dockerfile.airport'
    - '-t'
    - 'gcr.io/$PROJECT_ID/airport-autoresearch:latest'
    - '.'
  timeout: '1200s'

images:
- 'gcr.io/$PROJECT_ID/airport-autoresearch:latest'

timeout: '1500s'
