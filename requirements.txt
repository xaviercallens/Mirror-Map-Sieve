# Mirror Map Sieve — Python Dependencies
# Verified: Python 3.11+

# Core mathematics
sympy>=1.12          # Exact rational arithmetic, nullspace solver
mpmath>=1.3.0        # Arbitrary-precision arithmetic
fractions             # Built-in, used in verify_mirror_map.py

# Scientific computing
numpy>=1.26          # Array operations for benchmarks

# Deep learning (optional — required for GPU kernel benchmarks)
# GPU kernels run on L4/T4/A100. For CPU-only mode, torch is not required.
torch>=2.2.0          # PyTorch (for callens_alix/lia/al kernels)
# triton>=2.2.0       # Triton (optional, for custom CUDA kernels on A100)

# HTTP (Mistral peer review)
requests>=2.31        # HTTP client for Mistral API

# Utilities
tqdm>=4.66            # Progress bars
